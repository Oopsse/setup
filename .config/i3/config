# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod1
set $alt Mod4

font pango:Iosevka Aile 8

set $ws1 1:
set $ws2 2:
set $ws3 3:
set $ws4 4:
set $ws5 5:󱀣
set $ws6 6:
set $ws7 7:󰒋
set $ws8 8:󰷈
set $ws9 9:󰕼
set $ws10 0:󰌋

set $bgcolor 393939
set $fgcolor 48B685
set $txtcolor DFEFE2
set $urgent 48B685
set $transparent #00000000

#exec --no-startup-id exec /usr/local/bin/autorandr -c
exec --no-startup-id $HOME/.config/i3/tray
exec --no-startup-id $HOME/.config/i3/i3-battery-popup

exec --no-startup-id /usr/bin/xss-lock -n /usr/lib/xsecurelock/dimmer -l -- xsecurelock
exec --no-startup-id /usr/bin/autorandr --change
exec --no-startup-id /usr/bin/xinput set-prop "DLL0945:00 04F3:311C Touchpad" "libinput Tapping Enabled" 1
exec --no-startup-id /usr/bin/lxpolkit
exec --no-startup-id /usr/bin/picom -b --config $HOME/.config/picom/config
exec --no-startup-id /usr/bin/feh --bg-fill $HOME/Images/2.jpg

#exec --no-startup-id $HOME/.config/polybar/launch.sh

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

bindsym XF86MonBrightnessUp exec "/usr/bin/xbacklight +10"
bindsym XF86MonBrightnessDown exec "/usr/bin/xbacklight -10"

#bindsym XF86AudioRaiseVolume exec "/usr/bin/pactl set-sink-volume 1 +5%"
#bindsym XF86AudioLowerVolume exec "/usr/bin/pactl set-sink-volume 1 -5%"
#bindsym XF86AudioMute exec "/usr/bin/pactl set-sink-mute 1 toggle"

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Fermer (tuer) la fenetre ayant le focus:
bindsym $mod+Shift+Q kill
bindsym $mod+F4 kill

# alt+tab qui switch entre last et next focus window
bindsym $mod+Tab workspace back_and_forth

# Deplacer une window dans un autre ecran (Win+Left/Right)
bindsym $alt+Left move workspace to output left
bindsym $alt+Right move workspace to output right
bindsym $alt+Up move workspace to output up
bindsym $alt+Down move workspace to output down

# Change focus
bindsym $alt+j focus left
bindsym $alt+k focus down
bindsym $alt+l focus up
bindsym $alt+m focus right

# Alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+M move right

# Alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Split in horizontal orientation
bindsym $mod+Shift+h split h

# Split in vertical orientation
bindsym $mod+Shift+v split v

# Enter fullscreen mode for the focused container
bindsym $alt+f fullscreen toggle

# Change container layout (stacked, tabbed, toggle split)
bindsym $mod+Shift+s layout stacking
bindsym $mod+Shift+z layout tabbed
bindsym $mod+Shift+e layout toggle split

# Toggle tiling / floating
bindsym Ctrl+space floating toggle
for_window [floating] border normal
for_window [class="VirtualBox Machine"] border normal
for_window [class="VirtualBox Machine"] layout tabbed
for_window [class="VirtualBox Manager"] border normal

for_window [class="(?i)arandr"] floating enable
for_window [class="(?i)aggressor-Aggressor"] floating enable

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+q focus parent

# Raccourci pour alterner entre les modes de bordures (normale, 1 pixel ou aucune)
bindsym $mod+Shift+b border toggle

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10


# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Control+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym m resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# Bordure des nouvelles fenêtres (normal | none | 1pixel | pixel n)
new_window none

assign [class="(?i)firefox"] $ws1
assign [class="(?i)terminator"] $ws2
assign [class="(?i)brave-browser"] $ws3
assign [class="(?i)burp"] $ws4
assign [class="(?i)nautilus"] $ws6
assign [class="(?i)virtual"] $ws7
assign [class="(?i)docker desktop"] $ws7
assign [class="(?i)freerdp"] $ws7
assign [class="(?i)pavucontrol"] $ws0
assign [class="(?i)thunderbird"] $ws8
assign [class="(?i)obsidian"] $ws8
assign [class="(?i)vlc"] $ws9
assign [class="(?i)gnome-control-center"] $ws9
assign [class="(?i)keepassxc"] $ws10

# Definition des raccourcis d'applications (avec bindsym)
bindsym $mod+Return exec terminator
bindsym $mod+f exec firefox-esr
bindsym $mod+p exec librewolf
bindsym $mod+b exec docker start burppro
bindsym $mod+e exec thunar
bindsym $mod+v exec vlc
bindsym $mod+m exec vmware
bindsym $mod+s exec flameshot
bindsym $mod+c exec chromium
bindsym $mod+t exec thunderbird
bindsym $mod+k exec keepassxc
bindsym $mod+l exec xsecurelock
bindsym Control+$mod+F4 exec --no-startup-id systemctl poweroff -i, mode "default"
bindsym $mod+d exec --no-startup-id rofi -no-config -no-lazy-grab -show drun -modi drun -theme ~/.config/i3/launcher.rasi

set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id $Locker, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id $Locker && systemctl suspend, mode "default"
    bindsym h exec --no-startup-id $Locker && systemctl hibernate, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Shift+s exec --no-startup-id systemctl poweroff -i, mode "default"  

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+x mode "$mode_system"

bar {
strip_workspace_numbers yes
  padding 2px 6px 4px 1px
  position top
  status_command i3status
  tray_output primary
  workspace_buttons yes
i3bar_command i3bar --transparency
status_command i3status
colors {
background $transparent
statusline #ffffff
separator #FFFFFF

focused_workspace #4c7899 #285577 #ffffff
active_workspace #333333 #5f676a #ffffff
inactive_workspace #333333 #222222 #888888
urgent_workspace #2f343a #900000 #ffffff
binding_mode #2f343a #900000 #ffffff
}  workspace_min_width 40
}
